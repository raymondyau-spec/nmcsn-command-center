# NMCSN Command Center (public-safe)
Same page as `site/`, with Ray's private engines removed (estate, investments, NewCo) so it can be hosted on a public GitHub Pages site. `index.html` re-reads `board.json` every 30 s. One board, one writer: whoever owns this repository owns `board.json`.
